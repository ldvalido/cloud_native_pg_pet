apiVersion: v1
kind: Secret
metadata:
  name: cnpg-app-secret
  namespace: postgres
type: kubernetes.io/basic-auth
stringData:
  # REPLACE these values before applying to any real environment.
  # For production, use Sealed Secrets, External Secrets Operator, or Vault.
  username: app
  password: CHANGE_ME_APP_PASSWORD
